# Ingest Workflow

This workflow acquires public NCBI records and writes curated segment-specific metadata and FASTA files for downstream phylogenetic builds.

Run from the repository root:

```bash
nextstrain build ingest
```

The default target is `Orthohantavirus andesense`, NCBI taxon `1980456`.
